<DeviceProperties>
	<Joint type="${jointTouque}"
		RPM="${jointRPM}"
		Resolution="${jointResolution}"
		Voltage="${jointVoltage}"
	/>
	<Link Length="${linkLength}"
		Mass="${linkMass}"
	/>
	<DeviceMode type="${devMode}"
	/>
</DeviceProperties>
	
